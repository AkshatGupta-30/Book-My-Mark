import React from "react";
import ReactDOM from "react-dom/client";

ReactDOM.createRoot(document.body.appendChild(document.createElement("div"))).render(
	<React.StrictMode>
		<h1 style={{color: '#00f'}}>Options</h1>
	</React.StrictMode>,
);
